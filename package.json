{
  "name": "claude-code-workflows",
  "version": "0.22.7",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "packageManager": "pnpm@10.28.2",
  "scripts": {
    "prepare": "lefthook install",
    "sync": "node scripts/sync-plugins.mjs",
    "sync:check": "node scripts/sync-plugins.mjs --check && node scripts/check-agent-references.mjs",
    "check:agent-references": "node scripts/check-agent-references.mjs",
    "check:skills-index": "node scripts/check-skills-index.mjs"
  },
  "devDependencies": {
    "lefthook": "^2.1.6"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "lefthook"
    ]
  }
}
